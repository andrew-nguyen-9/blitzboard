"""`blitz_engine.testing` — the E7a league-config matrix loader (test-only scaffolding).

`matrix.py` reads the checked-in `fixtures/league_matrix.json`; nothing here regenerates it
(see `generate_matrix.py` for that).
"""
