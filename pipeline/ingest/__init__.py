"""Context/environmental ingest (E3).

Free-data ingestion that hydrates the ``metadata`` escape-hatch the E3 projection
factors (``models/factors/environment.py`` + ``scheme.py``) read. See
``context_ingest`` for the F2-style adapter + the artifact E9b (Articles) consumes.
"""
