# Epic 1 — Pipeline minutes cut — DONE

**Status:** done. Branch `v3-epic-1-pipeline-minutes` → PR base `v3`.

**Shipped:** Cut daily ETL CI minutes via caching + a skip-when-cached gate on the
heavy nflverse history step (kept `ubuntu-latest`, no hardware change).

**Changed/new files:**
- `.github/workflows/etl_daily.yml` — added explicit pip `cache-dependency-path`;
  a "Determine history scope" step (sanitizes the untrusted dispatch input via env,
  computes `2022..latest-completed-season` on cron); `actions/cache` for the marker
  keyed by the season list; history step now passes `--skip-if-cached` on cron only.
- `pipeline/history_ingest.py` — `--marker`/`--skip-if-cached`; writes a marker of
  ingested seasons after a successful run; early-exits (no download/upsert) when the
  marker already covers the requested seasons. Idempotent.
- `pipeline/tests/test_history_marker.py` — plain-assert check of the cover/write logic.
- `.gitignore` — ignore `pipeline/.nflverse_cache/`.

**Key decisions / gotchas for dependents (Epic 3.1 snapshot, Epic 12 models):**
- "New season detected" = season list changes ⇒ new cache key ⇒ miss ⇒ re-ingest.
  Manual `workflow_dispatch` always re-runs (no skip flag). Marker lives in cached
  `pipeline/.nflverse_cache/marker.json`.
- In-season weekly history updates are NOT auto-run by cron (run-once-then-skip per spec);
  use `workflow_dispatch` or `--weekly`. The 11-season backtest stays out of daily CI (Epic 12).
- Frontend untouched — its build/tsc/vitest DoD is inherited green from `v3`.
