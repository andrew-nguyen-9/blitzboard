# Epic 9 — Waiver (unauth) — DONE

**Status:** done. Branch `v3-epic-9-waiver-unauth` → PR base `v3`.

**Shipped:** The public `/waivers` page is the all-NFL unauth waiver tool — FAAB board (trending)
+ an **RSS "NEWS PULSE" feed scoped to all NFL** (`getRecentNews`, league-agnostic). Added a
`Scope: All NFL` header chip and an `All NFL` label on the RSS feed. Made the page resilient: it
no longer dead-ends to the empty state when `trending` is sparse — it renders whenever there's a
board **or** news, so the all-NFL RSS feed never gets hidden.

**Files:** `frontend/app/waivers/page.tsx` (only change). No new components/queries — reused
`WaiverBoard`, `getWaiverTargets`, `getRecentNews`. No migration (no new table).

**Decisions:** RSS = the NEWS PULSE sidebar (all-NFL news), per the v3 spec. Reduced-motion is
handled globally in `globals.css`; only CSS hover transitions on this page — no new motion.

**For Epic 8 (auth waiver):** the `Scope: All NFL` chip is the single insertion point (marked
`// ponytail:`) — swap it for `<LeagueSelector/>` + the all-NFL ↔ league/team scope toggle, and
pass a roster filter into `WaiverBoard`/`getRecentNews` for the league/team-specific feed.

**DoD:** build clean, `tsc --noEmit` clean, vitest 143 green, bundle audit no-secrets, Playwright
screenshot confirmed the board + all-NFL RSS feed render with real data (trending=205, news=139).
