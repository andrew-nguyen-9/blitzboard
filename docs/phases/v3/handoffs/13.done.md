# Epic 13 — Header (done)

**Status:** done.

**Shipped**
- Theme is now **light/dark only — default dark**. Removed the `auto`/`system` option.
- `ThemeToggle.tsx`: two-state icon button (sun when dark→light, moon when light→dark), aria-label per state. Migrates any legacy stored `system`/`auto` in localStorage `ffdt-theme` → `dark` in place.
- `ThemeScript.tsx`: no-flash default is `dark`; only an explicit `light` opts out (no more matchMedia branch).
- `Nav.tsx`: signed-out → **profile icon → /login**; signed-in → **settings gear → /account** (auth only). One icon at a time (reconciles Epic 6's profile→/account into the gear).
- `A11ySettings.tsx`: summary is now an **accessibility-glyph icon button** (was "⚙ Access") so it doesn't clash with the new settings gear.
- `lib/auth/prefs.ts`: theme enum dropped `system`, default `dark`, legacy/invalid values coerce to `dark` via `.catch` (no throw). Tests updated.

**Decisions / gotchas**
- Icons are **inline SVG** (no `lucide-react` in deps; didn't add one). currentColor → theme-adaptive.
- Account-synced theme `system` values now load as `dark` (migration via `.catch`), not an error.
- DoD green: `tsc --noEmit`, `vitest` (160), `next build` all clean; header screenshot captured dark (default) + light, unauth. Authed gear branch is a server-side ternary (build/type-verified; no test session available).
