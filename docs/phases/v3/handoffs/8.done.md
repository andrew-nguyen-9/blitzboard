# Epic 8 — Authenticated section — DONE

**Status:** done (ESPN rule-import gated — see gotchas). Branch `v3-epic-8-authenticated` → PR base `v3`.

**Shipped (all 5 surfaces):**
- **Leagues** `/leagues` (`LeagueManager.tsx`) — connect up to 3 via Sleeper (full rules) + ESPN, set active, disconnect. Reuses `LeagueImport.tsx` + `connectLeague`/`setDefaultLeague`/`disconnectLeague` actions (RLS per `auth.uid()`).
- **Draft** — `DraftRoom.tsx` gains `savedLeagues` prop: hides the Manual/Sleeper/ESPN selector + import, shows a 2-3 league toggle that swaps each league's stored `LeagueConfig`. Wired in `app/draft/page.tsx` via `getMyLeaguesWithConfig()`.
- **Waiver** `WaiverScope.tsx` — `LeagueSelector` + all-NFL↔free-agents toggle (default free agents) over `WaiverBoard`; pure `lib/waiverScope.ts` (+test) does the FA/news filter.
- **Trades** — `TradeCalculator.tsx` takes optional `leagues`: `LeagueSelector` replaces the Scope chip; RSS labels the league then refocuses to traded players on submit.
- **Settings** `/account` (`AccountSettings.tsx` + `actions/account.ts`) — change email/password/phone (phone envelope-encrypted to `accounts.phone_encrypted`), 2FA link (`/auth/2fa`), delete account.

**New tables/migrations:** no new tables (reuses `user_leagues`/`league_rules`/`accounts`/`credential_vault`, all RLS'd). One migration `db/migrations/20260630_v3.8.1_account_delete.sql` — `delete_my_account()` SECURITY DEFINER RPC (scoped to `auth.uid()`, cascades clean dependents), `grant execute to authenticated`.

**League context mechanism:** league rules stored as jsonb in `league_rules.config` (full `LeagueConfig` for Sleeper, default shell for ESPN); `getMyLeaguesWithConfig()` joins them; pages branch on a signed-in session with ≥1 league.

**Gotchas / gated:** ESPN has no public settings→rules proxy, so an ESPN league stores a default editable shell (rules adjustable in the draft room) + its private cookies as one encrypted `credential_vault` secret (`{s2,swid}` JSON) — full ESPN rule import is the upgrade path. Authed-surface screenshots are gated: needs the multi_league migration applied to the DB + a confirmed-auth session; verified instead via tsc/build (routes compile), gate redirects, unauth fallbacks render, and graceful empty-league degradation. `leak-boundary.test` now scans browser-shipped client components (the waivers/trades pages became auth-aware server components). DoD green: build, `tsc --noEmit`, vitest 155, bundle audit (no secrets).
