# Epic 12 — Models (VORP + Monte Carlo) — DONE

**Status:** done (Supabase upload gated locally — see below). Branch `v3-epic-12-models` → PR base `v3`.

**Shipped:** (12.2 net-new) `SeasonSimulator` — injury/usage/variance → per-player season floor/ceiling/boom-bust; (12.1) out-of-sample backtest of VORP + the simulator vs nflverse actuals 2015-2025, idempotent Supabase upload. Heavy run is LOCAL / manual-dispatch only, never daily CI.
**Files:** `pipeline/models/season_sim.py`, `pipeline/backtest/models_backtest.py`, `db/migrations/20260630_v3.12.1_model_backtest.sql`, `.github/workflows/models_backtest.yml`, `pipeline/tests/test_season_sim.py`+`test_models_backtest.py`, `models/__init__.py`.
**MC schema** (`SeasonOutcome`): `player_id, position, proj_mean, floor`(P10)`, p50, ceiling`(P90)`, boom_bust, boom_pct, bust_pct, games_exp`. Live per-player values ship via the `monte_carlo` snapshot (manifest `default:monte_carlo`), NOT the table below.
**Results table** `model_backtest` (PK `model,season`; `season=0`=2015-2024 summary; `metrics` jsonb, RLS public-read). VORP spearman(value,actual)=**0.59** (0.53–0.66/yr), top24 prec 0.29. MC P10–P90 coverage **0.37**, MAE ~51, spearman(mean,actual) 0.63. 2025 weekly data not on nflverse mirror yet → auto-skipped (non-fatal).
**Upload gating (blocked step):** local `.env.local` is anon-only (no `SUPABASE_SERVICE_ROLE_KEY`) ⇒ models computed+validated, upload **dry-ran** (would upsert 22 rows). Manual workflow `models_backtest.yml` holds the CI secret and writes. Upsert idempotent on `(model,season)`.
**For Epic 11 (player page):** floor/ceiling/boom-bust come from the `monte_carlo` snapshot, not `model_backtest`; boom_pct/bust_pct are probabilities, floor/ceiling are P10/P90.
**For Epic 14 (About):** cite `model_backtest` `season=0` (VORP ρ≈0.59, MC coverage≈0.37). Coverage <0.80 is real 1-yr-lookback fantasy volatility — the simulator self-test hits 0.81 on a true input; `PROJ_DRIFT=0.5` is a backtest-only widening knob, not used live.
