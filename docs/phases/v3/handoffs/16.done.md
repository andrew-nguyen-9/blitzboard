# Epic 16 — Mobile (US-017) — done

Status: **done**. Branch `v3-epic-16-mobile`, PR base `v3`.

QA'd every public route at 390 / 768 / 1280 via Playwright (overflow audit + screenshots).
The v2 design system already made most pages responsive (fluid clamps, `.stat-table` stacking,
deck-rail). Two real breaks found and fixed:

- **WaiverBoard.tsx** — the `lg:grid-cols-[1fr_300px]` track's default `min-width:auto` let the
  6-col table force the page wide on mobile (711px). Fix: `min-w-0` on the component root + table
  card switched `overflow-hidden`→`overflow-x-auto` (table scrolls in-card, `min-w-[40rem]`). Also
  fixes the authed WaiverScope path (reuses WaiverBoard).
- **Nav.tsx** — section links were `hidden md:flex` with no mobile fallback (only reachable via
  footer). Added a native `<details>` hamburger menu (`md:hidden`), reusing the `.a11y`
  marker-reset + panel styling. No client JS (Nav stays a Server Component); keyboard-operable.

DoD: `npm run build` clean, `tsc --noEmit` clean, vitest 162 green. No new animation (reduced-motion
unaffected); menu has aria-label, real `<Link>`s. No new deps, no env/secret touched.
Gotcha (pre-existing, not this epic): local Supabase lacks `user_leagues` table → `getMyLeagues`
console error on data pages; gated migration per US-008/US-012, harmless.
