# Epic 10 — Trades (unauth) — DONE

**Status:** done. Branch `v3-epic-10-trades-unauth` → PR base `v3`.

**Shipped:** `/trades` is now the public all-NFL **trade calculator** — search any player from the
CDN snapshot, stack both sides, see the value verdict + fairness. All-NFL **NEWS PULSE** RSS feed
(matches Epic 9: `getRecentNews`, `Scope` chip + feed label) that **refocuses to the traded
players on submit** (chip flips All NFL → This trade).

**New files:** `lib/tradeSearch.ts` (`PlayerSearchIndex` — prefix trie + subsequence fuzzy, with a
labeled `// ponytail:` char-mask bloom pre-filter), `lib/tradeSearch.test.ts` (7 tests).
`components/TradeCalculator.tsx` (client island; loads snapshot like PlayerTable, position + NFL-team
dropdowns, two sides, `Analyze trade`). **Changed:** `app/trades/page.tsx` (thin shell, passes news),
`lib/leak-boundary.test.ts` (added TradeCalculator to public-plane scan).

**Decisions:** value = snapshot `value` (fall back `vor`), summed per side; fairness = min/max. News
has no player_id, so refocus matches name tokens ≥4 chars (`// ponytail:`, upgrade = player_id join).
The old P5 league Pareto `TradeFinder.tsx` + `lib/trade.ts` are left intact for Epic 8 to build on.

**For Epic 8 (auth trades):** layer on top of `TradeCalculator` — the `Scope` chip is the insertion
point (swap for `<LeagueSelector/>` + all-NFL ↔ league/team toggle); add roster multi-select (reuse
`getLeagueTeams`/`getPlayersWithValueByIds`) and team-focused RSS (filter `getRecentNews`).

**DoD:** build clean, `tsc --noEmit` clean, vitest 151 green, bundle audit no-secrets, Playwright
screenshot confirmed search + filters + submit refocus on real data (Lamar 198 vs Bijan 171, feed → "This trade").
