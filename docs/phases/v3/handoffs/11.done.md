# Epic 11 — Player page — DONE

**Status:** done. Branch `v3-epic-11-player-page` → PR base `v3`.

**Shipped:** Fleshed out the player detail page from existing pipeline data only. The
per-season `stats` jsonb (previously dropped — only fantasy_pts shown) now drives a
position-aware **Career Production** table (QB/RB/WR·TE splits + G, Pts, PPG) with a
career-high / career-PPG / YoY-delta summary. Value card gained tooltip-defined metrics
(VOR, Repl, ADP) via the shared `@/components/Tooltip` and a **simulated outcome band
(P10–P90)** from the stored boom/bust. Identity adds yrs-exp + depth-chart order.

**Files:** `frontend/lib/playerStats.ts` (+`.test.ts`, pure position-aware career helpers),
`frontend/app/players/[id]/page.tsx` (rewrite — reuses StatTable, Tooltip, DistributionBar,
Sparkline, ValueDial, PredictabilityMeter; no new data source, no new query).

**Data surfaced:** VORP value/rank/vor/replacement/adp/boom/bust/predictability, ensemble
projection (mean/floor/ceiling/σ + model inputs), per-season box score + fantasy trend.

**Gotcha (Epic 12 blocker):** `monte_carlo` engine has **0 live rows** and weekly logs aren't
published (both gated to CI). The MC toggle now **gracefully gates**: shows a note and falls
back to VORP with its simulated band. When the model workflow uploads MC rows, the toggle
lights up with no page change. Weekly-log section intentionally skipped (no live rows).

**DoD:** build clean, tsc clean, vitest 160 green (5 new), reduced-motion + a11y via reused
primitives, populated-page screenshot (Jalen Hurts QB) confirmed.
