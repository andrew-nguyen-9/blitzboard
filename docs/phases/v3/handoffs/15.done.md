# Epic 15 — SEO · DONE

Status: **done**. Branch `v3-epic-15-seo` → PR base `v3`. (PRD id: US-016.)

Shipped (native Next.js App Router metadata APIs, no new dep):
- `app/robots.ts` — allow `/`, disallow `/account /leagues /login /signup /auth/ /api/ /kit`, sitemap + host.
- `app/sitemap.ts` — 9 static public routes + DB-enumerated `/players/[id]` pages; absolute URLs, lastmod/changefreq/priority. Null-safe → static-only with no keys.
- `lib/queries.ts` — `getSitemapPlayerIds()` (id-only read, capped 5000; Supabase-reads-here convention).
- `app/players/[id]/page.tsx` — `generateMetadata` now emits a player-specific description + OG (`type: profile`) instead of inheriting the generic tagline.
- `app/sitemap.test.ts` — mapping check (static + player URLs, no private-route leak, no-keys degrades to 9).

Decisions / gotchas:
- Non-indexing of private/utility routes handled once in robots.ts (disallow list) rather than per-page noindex — shortest correct point.
- Layout already supplies default title-template + OG + `/opengraph-image.png`; per-route pages only override title (+ player desc). No `metadataBase` change.
- Build verified: `/robots.txt` + `/sitemap.xml` prerender (sitemap pulled ~1000 real player ids locally). tsc clean, vitest 162 green, `npm run build` clean. No UI/animation → no reduced-motion/a11y surface; verified via rendered build output, not screenshot.
