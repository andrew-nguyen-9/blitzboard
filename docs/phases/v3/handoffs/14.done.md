# Epic 14 — Footer — DONE

**Status:** done. Branch `v3-epic-14-footer` → PR base `v3`.

**Shipped:** Global `Footer` component (replaces the inline `<footer>` in `app/layout.tsx`) + three content routes: About, Privacy, Terms.
**Files:** `frontend/components/Footer.tsx`, `frontend/app/about/page.tsx`, `frontend/app/privacy/page.tsx`, `frontend/app/terms/page.tsx`, `frontend/app/layout.tsx` (wires Footer).
**Footer:** brand + attribution block, "Sections" nav (reuses `Nav.SECTIONS` — single source of truth, no duplication), "Info" group (About & methodology, Privacy, Terms, an9.dev ↗), © line linking an9.dev.
**About says:** methodology-depth copy. VORP = value over replacement (projection vs replacement-level player at position, makes positions comparable by scarcity). Monte Carlo = simulated seasons → per-player season-long distribution → floor (P10), ceiling (P90), boom/bust probabilities. Models validated out-of-sample vs historical actuals. NO numeric accuracy figures printed.
**Decisions:** (1) Per orchestrator + 14.md, deliberately omitted the 12.done.md backtest numbers (ρ≈0.59, coverage≈0.37) — "no unverified accuracy numbers" wins; described the *method* of validation instead. (2) Privacy adapted from `../soundcheck` but corrected: BlitzBoard DOES have accounts (Supabase Auth), so privacy covers stored email/auth + per-account league/roster isolation. (3) No animation in footer → nothing to gate for reduced-motion.
**Gotchas for dependents:** site-pages nav is driven by `Nav.SECTIONS`; add a route there and it appears in both nav and footer automatically. Footer is a server component importing the `SECTIONS` const from `Nav` (no client cost).
**DoD:** `tsc --noEmit` clean, `vitest` 160 green, `npm run build` clean (/about, /privacy, /terms all present), Playwright screenshots of all four surfaces confirmed render.
