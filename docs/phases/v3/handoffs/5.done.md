# Epic 5 — League page → auth-only (done)

**Status:** done. Reused Epic 6/7 auth plumbing — no new auth system.

**Shipped / changed files**
- `lib/auth/redirect.ts`: `isProtectedPath(pathname)` — prefix list `["/league"]`, path-boundary safe (+tests).
- `lib/supabase/middleware.ts`: `updateSession` now captures `getUser()`; `!user && isProtectedPath(...)` → `307` to `/login?next=<path>`.

**Mechanism (matcher / return-to)**
- Matcher unchanged: top-level `middleware.ts` config already runs on `/league`; the gate rides the existing session-refresh pass — no new matcher entry.
- Return-to: middleware sets `?next=/league` → `/login` hidden input → `signInWithEmail` runs it through `safeNext` (open-redirect guard) → back to `/league`.
- Offline (no Supabase env): middleware passes through; page's own `resolveGateState()` still renders the `ConnectPrompt` login state — soft-gate kept as defense-in-depth.

**Gotchas for Epic 8 (auth section)**
- Add authed routes by appending a prefix to `PROTECTED_PREFIXES`. No new tables/migrations/RLS; bundle clean (anon key only).
- Authed-render path unchanged (covered by `gate.test.ts`); no test creds provisioned → authed screenshot deferred, unauth redirect is screenshot-verified.
- DoD: `next build` 0 err, `tsc --noEmit` clean, `vitest` 153 green; Playwright `/league` → `/login?next=%2Fleague`.
