# Epic 2 — Homepage — DONE

**Shipped:** Deck copy fixed (`seven`→`six tools`), a dependency-free CSS scroll-snap deck
carousel, and a static football-play "blueprint" hero background. Build/tsc/vitest all green;
verified live with Playwright (1280 + 390px), tooltip, and reduced-motion still-state.

**Files**
- `frontend/components/BlueprintField.tsx` (new) — static SVG blueprint (grid, line of scrimmage, X/O marks, drawing routes).
- `frontend/app/page.tsx` — copy fix, mount `BlueprintField` (right-weighted), deck → `.deck-rail` carousel.
- `frontend/components/TiltCard.tsx` — `h-full` so cards fill the rail cell (equal size).
- `frontend/app/globals.css` — `.deck-rail` (single-row grid + scroll-snap) and `.bp*` (route draw + reduced-motion still-state).

**Rive status:** STATIC FALLBACK (expected blocker per §3). `.riv` authoring needs the Rive
editor GUI (binary format, not headless-authorable). Shipped a CSS/SVG blueprint instead — routes
draw via `stroke-dashoffset`, collapsing to a fully-drawn still frame under reduced motion.
Upgrade path noted in `BlueprintField.tsx`: swap to `<RiveInstrument fallback={<BlueprintField/>}>`.

**Decisions / gotchas**
- Carousel is a single-row CSS grid (`grid-auto-flow: column`): stretches all cards to equal w+h, no JS, no carousel dep. ~1.1 cards peek mobile, 3 desktop.
- Deck tooltip was already wired in Epic 3.2; only the copy changed. Cards stay links (keyboard-scrollable).
- `--space-xs` does not exist in the token scale — use `--space-2xs`/`-s`/`-m`.
