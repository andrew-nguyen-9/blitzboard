# Epic 3.1 — Publish real snapshot — DONE

**Status:** done. Branch `v3-epic-3.1-publish-snapshot` → PR base `v3`.

**Shipped:** Verified a real, current player snapshot is live on Supabase and the
players page renders it — the "No snapshot published yet" empty state is gone.
Live: `player_value` (vorp) = 4254 rows; published blob = 4254 players. Playwright
confirmed real rows (Jalen Hurts T1 QB 205.1, Lamar, Chase, Bijan…) with tiers/VOR/ρ/trend.

**Files:** docs only (`3.1.done.md`). No code change — pipeline (`value_engine_run.py`,
`publish_snapshot.py`) and reader (`frontend/lib/snapshot.ts`) already existed and work.

**For dependents (4/9/10/8/11/12):**
- Storage bucket **`snapshots`**, manifest **`manifest.json`** (60s TTL), blobs
  `players-<profile>-<engine>-<hash>.json.gz` (gzip, immutable, content-hashed).
- Manifest key = **`{profile}:{engine}`** (e.g. `default:vorp`) → `{url,hash,count}`.
- Page reads via `lib/snapshot.ts loadSnapshot()`: GET manifest (no-store) → resolve url →
  fetch gzip → `DecompressionStream('gzip')` → `decodeSnapshot` (columnar cols
  `sid,n,pos,tm,val,vor,rnk,rho,trend`, resolved by name). Null-safe → empty state on miss.
- **Gotcha:** re-publishing requires `SUPABASE_SERVICE_ROLE_KEY`, which is **not** in local
  `frontend/.env*` (anon-only by policy) — it lives only in GitHub Actions secrets. Local
  re-run runs dry. Snapshot was already current (idempotent: same data → same hash), so the
  goal is met; refresh runs via CI/etl with the service-role secret. DoD green: build/tsc/vitest, bundle audit (no secrets), UI screenshot.
