# Epic 17 — Perf (US-018) — done

Status: **done**. Branch `v3-epic-17-perf`, PR base `v3`.

Lighthouse (mobile, prod build, `/`): **A11y 96→100, Best Practices 100, SEO 100, Agentic 100**.
Perf trace: **LCP 599 ms, CLS 0.00, TTFB 267 ms**, no render-blocking savings available — LCP is
server-rendered hero text, framer no longer in the critical path.

Shipped:
- **framer-motion → `LazyMotion` + `m`** (new `components/MotionProvider.tsx`, wraps the layout
  body; `strict` + `domAnimation`). Swapped `motion.*`→`m.*` in `motion.tsx` + `TiltCard.tsx`.
  Framer's DOM features now load as an async chunk, out of First Load JS. **First Load JS: `/`
  150→136 kB, `/kit` 145→131 kB, `/players/[id]` 154→140 kB (−14 kB each).** Reduced-motion gates
  unchanged (same API), 0 console errors.
- **a11y fix** `Footer.tsx`: creator link was color-only (WCAG link-in-text-block fail) → always
  underlined. Took `/` accessibility 96→100.

Decisions / not done (ponytail): app was already lean — lenis/gsap lazy, fonts subset+selective
preload, server-rendered LCP, react-virtual tables, CSS-only hero. No image-format change (only
remote next/image is an 84px team logo — AVIF gain negligible). Left unused `@rive-app` dep +
`RiveInstrument.tsx`: tree-shaken (0 client bytes), documented Epic-2 swap-in scaffold.

DoD: `npm run build` clean, `tsc --noEmit` clean, vitest **162 green**, bundle secret-audit clean,
Playwright screenshot + Lighthouse verified. No new deps, no env/secret touched.
