# Epic 6 — Login (done)

**Status:** done. Built on existing v2.5 Supabase Auth plumbing (login/signup/OAuth/forgot-password already shipped); Epic 6 added TOTP 2FA + the header profile icon.

**Shipped**
- TOTP 2FA opt-in: `app/auth/2fa/page.tsx` (enroll QR + secret, confirm, turn-off), `app/auth/2fa/verify/page.tsx` (sign-in step-up).
- `app/actions/auth.ts`: `signInWithEmail` now checks AAL and redirects to `/auth/2fa/verify` when a verified factor exists; added `enrollTotpVerify` / `verifyTotp` / `disableTotp`.
- `lib/auth/mfa.ts` (+test): pure `needsMfaStepUp` / `verifiedTotpFactor` decisions.
- `lib/auth/sms.ts` (+test): SMS-sender interface stub — **unconfigured** (`SMS_PROVIDER` unset → `getSmsSender()` null, `sendSmsOtp` rejects). This is the wired SMS seam.
- `components/Nav.tsx`: now async; header profile icon → `/login` (signed-out) / `/auth/2fa` (signed-in).
- Forgot-password unchanged: login → `/auth/update-password` (email reset via `resetPasswordForEmail`).

**Key decisions / gotchas for dependents (7/5/8/13)**
- **No new tables / migrations** — Supabase Auth owns MFA factors in its `auth` schema. No RLS work. Bundle audit clean (anon key only).
- **Session handling**: cookie-based via `lib/supabase/server.ts` + `middleware.ts` (`updateSession`) — unchanged; reuse `getServerSupabase()` server-side.
- **Auth routes**: `/login`, `/signup`, `/auth/callback`, `/auth/confirm`, `/auth/update-password`, `/auth/2fa`, `/auth/2fa/verify`. Server actions in `app/actions/auth.ts`.
- **Epic 13 (header)**: profile icon is a minimal seam in `Nav.tsx` (links only, no menu) — flesh out the account menu there. **Epic 8 (auth section)**: 2FA management lives at `/auth/2fa`.
- **SMS**: when SMS 2FA ships, set `SMS_PROVIDER`, implement `SmsSender` in `lib/auth/sms.ts`, enable phone factors in Supabase.
- DoD: `npm run build`, `tsc --noEmit`, `vitest` (125) all green; axe 0 violations on login + forgot-password; screenshots captured.
