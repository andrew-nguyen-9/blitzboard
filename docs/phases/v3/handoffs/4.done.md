# Epic 4 — Draft (unauth) — DONE

**Status:** done. Branch `v3-epic-4-draft-unauth` → PR base `v3`.

**Shipped:** Pre-draft league-rules editor (default superflex 2QB; superflex/K/DST/FLEX/teams/PPR
toggles, imported leagues read-only). Bye column now fills + affects drafting. Auto-draft no longer
freezes. Soft K/DEF penalty so skill backups fill first. Sleeper/ESPN id input moved out of Manual
into the live option. Standalone analysis = full "Your team" lineup-by-slot + bye stacks + per-team
positional-strength bars. Verified live (Playwright): 192/192 auto-draft completes instantly, K/DST
drafted, byes render, bye-week stacks flagged.

**Files:** `lib/byeWeeks.ts`(+test), `lib/draftAI.ts` (candidatePool, kdstSoftPenalty),
`lib/leagueConfig.ts` (rulesFromConfig/rosterFromRules/applyRules), `components/DraftRoom.tsx`
(RulesEditor + id-move + freeze fix), `components/DraftAnalysis.tsx` (MyTeam/byeStacks/posStrength),
`app/draft/page.tsx` (attachByes), `lib/draftAI.test.ts`.

**Auto-draft root cause + fix:** NOT the empty-snapshot path (verified — empty pool no-ops). With
the real 4254-player snapshot, `runSim` scored the *full* pool every pick; scoreBoard is O(pool²)
(each candidate re-walks the pool in expectedReplacementAtNextTurn) → billions of ops → main-thread
freeze. Fix: shared `candidatePool()` caps to top-80 by projection + a few K/DST, used by both
`runSim` and the recommend path (which was already capped — that asymmetry was the tell).

**Schedule import:** nflverse `games.csv` (2026, REG) → team→bye (missing week); aliases nflverse
`LA`→Sleeper `LAR`. `fetchTeamByes()` fetches live (3s budget, no-store, in-proc cached) with a baked
2026 `BYE_WEEKS_2026` fallback so the build/tests stay offline-deterministic. `attachByes()` fills
`players.bye_week` (Sleeper's feed never carries it) in the draft page server component.

**For Epic 8 (auth-draft):** reuse `candidatePool` for any sim; `applyRules`/`rosterFromRules` is the
canonical roster-shape builder; byes come from `attachByes(players, await fetchTeamByes())` — wire the
same call into the authed loader. DoD green: build, tsc, vitest (143), bundle audit (no secrets).
