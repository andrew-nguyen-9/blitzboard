# Epic 11 — Player page

> Self-contained handoff. Read this file and your injected `.done.md` notes; you do not need the
> master spec. Segment branch: **`v3-epic-11-player-page`**. Upstream notes to read: `3.1.done.md`, `3.2.done.md`, `12.done.md`.

## The work

Flesh out the player page **from existing pipeline data only** (no new source): weekly logs,
splits, trends, **VORP + Monte Carlo outputs** (from Epic 12), `Sparkline.tsx`. More stats, richer
layout. Use the shared tooltip primitive (Epic 3.2).

---

## §0 — Setup (every agent)

- Activate Serena on `/Users/andrewnguyen/Documents/GitHub/blitzboard`; use its symbolic tools
  (`find_symbol`, `get_symbols_overview`, `find_referencing_symbols`, `replace_symbol_body`) for
  code nav/edits over reading whole files. Serena memories already exist: `core`, `tech_stack`,
  `suggested_commands`, `conventions`, `task_completion` — read the relevant ones.
- **RTK** is hook-active: shell is auto-proxied (`git status` -> `rtk git status`). Don't fight it.
- **Supabase keys are provisioned** in `frontend/.env`, `frontend/.env.local`, and GitHub Actions
  secrets (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`). For local pipeline/script runs,
  source `frontend/.env.local`. **Never print, echo, or commit secret values.** Not a blocker.
- Read before touching code: `docs/workflow/VERSIONING.md`, `GIT_WORKFLOW.md`,
  `DEFINITION_OF_DONE.md`, `docs/architecture/ARCHITECTURE.md`.

## §1 — Definition of Done (the bar; non-negotiable)

A task is done only when ALL hold:
- `npm run build` clean (in `frontend/`), `tsc --noEmit` clean, `vitest` green.
- prefers-reduced-motion honored with a static fallback for every animation.
- RLS enabled + explicit policies on any new table; migrations timestamp-prefixed in `db/migrations/`.
- No secrets in the client bundle (service-role key = pipeline only; anon key = public reads).
- Accessibility checks pass; pipeline scripts idempotent.
- **UI tasks**: a Playwright/screenshot check confirming the change renders.
- **Ponytail discipline**: climb the ladder — reuse `frontend/lib` (`queries.ts`, `leagueRules.ts`,
  `crypto/`) and `components/` before writing new; native platform/CSS over deps; shortest correct
  diff. Mark deliberate shortcuts `// ponytail:` + upgrade path. Non-trivial logic leaves one
  runnable check (assert demo or `test_*`).

## §2 — Branch / commit / PR rules

- Phase branch is **`v3`** (off `main`). Work this epic on its own **segment sub-branch**
  (`v3-epic-11-player-page`) cut off `v3`.
- You are authorized to **commit, push, and open a PR autonomously** this run. PR base = `v3`.
- **Never** work on or push to `main`. Never jump ahead past a dependency.
- Commit messages + PR title/body: written as the user (no AI attribution, no Co-Authored-By,
  no "Generated with" footer).

## §3 — Blocker policy (autonomous)

On a blocker (missing repo, missing key, ambiguous spec, build you can't fix): **mark the task
blocked, log why, do what you can, and note it in your outcome file.** Do not halt. Reference repos
(`../portfolio-website`, `../soundcheck`) are local siblings — read them directly, not a blocker.
Supabase keys are NOT a blocker (provisioned, §0). The only likely hard blocker is Rive scene
authoring (Epic 2) -> ship the static fallback + log.

## §4 — Reference repos (local siblings, read in place — no clone/network)

- `../portfolio-website` -> hCaptcha / Vercel BotID / offline form-security source (Epic 7).
  Key files: `middleware.ts`, `app/api/contact/route.ts`, `components/Contact/index.tsx`.
- `../soundcheck` -> Privacy + Terms page structure (Epic 14). Key routes: `app/privacy/`, `app/terms/`.

## §5 — Outcome note (write before returning)

Write `docs/phases/v3/handoffs/11.done.md`, <=15 lines: what shipped, new/changed files, key
decisions, gotchas for dependents. This is the contract downstream epics read. Then return ONLY:
task id, status (done/blocked), branch, PR link, and a <=2-line note.
