# v2.0 — ideas & follow-ups for the backlog

Captured during the Foundation phase. Not committed work — candidates for later phases.

## Close-out follow-ups (cheap, do soon)

- **Flip the axe smoke to a blocking gate.** The shell is axe-clean as of v2.0.3, but the
  CI `a11y-smoke` job is still `continue-on-error: true`. Remove that one line to make it a
  real gate (it already passes green). Deferred intentionally in v2.0.3.
- **Add required status checks to branch protection.** `main` requires a PR but no required
  checks yet. Wire `build · typecheck · lint` and `no personal identity (D18)` into
  `branch-protection.json` (`required_status_checks.contexts`) so a red CI blocks merge.
- **Cache the Playwright browser in CI.** The axe job reinstalls Chromium each run; cache
  `~/.cache/ms-playwright` to shave ~30–60s.
- **Fix the two lint warnings.** `@next/next/no-page-custom-font` (layout loads fonts via
  `<link>`) and `react-hooks/exhaustive-deps` in `lib/useEspnSync.ts`.

## Token / design system

- **Migrate v1 feature components off the legacy aliases**, then delete the deprecated
  `--bg/--surface/--ink-muted/...` aliases from `globals.css`. Tracked debt from v2.0.2.
- **Replace hardcoded hex in v1 feature components** (`DraftRoom`, `WaiverBoard`,
  `TradeFinder`, `FootballPit`, `DraftAnalysis`) with the `--pos/--neg/--warn` semantic
  tokens — *required* for colorblind modes to actually affect those views (today the CVD
  ramps only retune tokens, which those components bypass).
- **Migrate font loading to `next/font`** (from the `<link>` in `layout.tsx`) for no layout
  shift + the perf budget in v2.1; also clears the lint warning.
- **OKLCH fallbacks**: relative-color syntax (`oklch(from …)`) and OKLCH are evergreen-only.
  Add an `@supports` sRGB fallback layer only if analytics show meaningful old-browser traffic.

## Accessibility

- **Account-sync a11y prefs** (text size / motion / contrast / CVD) in v2.5 — currently
  localStorage only.
- **A11y panel dismissal**: native `<details>` has no light-dismiss/Escape. Revisit the HTML
  Popover API once on React 19 (the `popover`/`popovertarget` typings land), for outside-click
  + Esc close.
- **Tabular-nums coverage**: applied to `<table>` + the utility; v1 stat displays built with
  divs need the utility explicitly. Re-audit when tables are rebuilt (v2.3).
- **Formalize the numeric-cell clipping test** (the v1 bug) once real data tables exist.

## Motion

- **Place a real `<RiveInstrument>` on the homepage (v2.1)**, replacing the public
  `sample.riv` with a bespoke instrument; verify the reduced-motion rest frame in situ.
- **Route-scope Lenis**: it's a dynamic import now (out of the global bundle) but still inits
  on every route after paint. Consider initializing it only on routes with scroll stories.

## Workflow / CI

- The `identity-guard` greps a fixed path list; revisit if the product surface area grows
  (e.g., a new top-level app dir) so nothing slips the net.
